# -*- coding: utf-8 -*-
"""
本地私有配置模板（config_local.example.py）
==============================================
使用方法：
    复制本文件为 config_local.py（同目录），然后填写实际值。
    config_local.py 已加入 .gitignore，不会提交到仓库。

所有敏感信息（邮箱授权码、Webhook、API密钥等）都应放在 config_local.py，
不要写入 config.py。

必填项（★标记）:
    ★ TOTAL_CAPITAL / AVAILABLE_CASH  — 账户资金
    ★ STOCK_POOL                      — 核心股票池
    ★ EMAIL_AUTH_CODE                 — QQ邮箱SMTP授权码
    ★ EMAIL_SENDER / EMAIL_RECEIVER   — 发件人/收件人邮箱
  可选项:
    DINGTALK_WEBHOOK                  — 钉钉机器人Webhook
    WECHAT_WORK_WEBHOOK               — 企业微信机器人Webhook
"""

# ============================================================
# ★ 必填：账户资金（券商口径）
# 获取方式：登录券商APP → 资产查询 → 查看总资产和可用资金
# ============================================================
TOTAL_CAPITAL = 500000       # 总资金（元）
AVAILABLE_CASH = 100000      # 当前可用资金（元）

# ============================================================
# ★ 必填：核心股票池
# 格式: {"股票代码": {"名称": "xxx", "赛道": "xxx", "类型": "龙头/弹性"}}
# 类型说明:
#   - "龙头": 主线赛道前3龙头，仓位上限15%
#   - "弹性": 非主线/弹性标的，仓位上限8%
# ============================================================
STOCK_POOL = {
    "600519": {"名称": "贵州茅台", "赛道": "白酒消费", "类型": "龙头"},
    "000858": {"名称": "五粮液",   "赛道": "白酒消费", "类型": "龙头"},
    # 按需添加更多标的...
}

# ============================================================
# ★ 必填：QQ邮箱SMTP授权码
# 获取方式：登录QQ邮箱 → 设置 → 账户 → POP3/SMTP服务 → 开启 → 生成授权码
# 注意：是16位授权码，不是QQ密码
# ============================================================
EMAIL_AUTH_CODE = "在此填写16位QQ邮箱授权码"

# ============================================================
# ★ 必填：发件人/收件人邮箱
# ============================================================
EMAIL_SENDER = "your_qq@qq.com"
EMAIL_RECEIVER = "your_qq@qq.com"

# ============================================================
# 可选：钉钉/企业微信机器人Webhook
# 获取方式：群设置 → 智能群助手 → 添加机器人 → 自定义 → 获取Webhook地址
# ============================================================
# DINGTALK_WEBHOOK = "https://oapi.dingtalk.com/robot/send?access_token=xxx"
# WECHAT_WORK_WEBHOOK = "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx"

# ============================================================
# 可选：覆盖其他config.py中的配置
# 直接在此追加需要覆盖的配置项，例如：
# ============================================================
# MAX_HOLDINGS = 8
# CASH_RESERVE_RATIO = 0.10
